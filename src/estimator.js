const covid19ImpactEstimator = (data) => { 
        const impact = () => {
            const reportedCases;
            const currentlyInfected = reportedCases*10;
            const threeDayPeriod;
            const infectionsByRequestedTime = currentlyInfected * Math.pow(2,threeDayPeriod);
        }
        const severeImpact = () => {
            const reportedCases;
            const currentlyInfected = reportedCases*50;
            const threeDayPeriod;
            const infectionsByRequestedTime = currentlyInfected * Math.pow(2,threeDayPeriod);
        }
}
export default covid19ImpactEstimator;
